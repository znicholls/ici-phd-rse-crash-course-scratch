"""
Unit aware operations
"""


def add_unit_aware(a: float, b: float) -> float:
    """
    Add two quantities in a unit-aware way

    Parameters
    ----------
    a
        First quantity

    b
        Second quantity

    Returns
    -------
    :
        Sum of `a` and `b`
    """
    return a + b
